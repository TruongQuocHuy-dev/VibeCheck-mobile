/**
 * Interface representing match details when 2 users connect.
 */
export interface MatchInfo {
  id: string;
  userName: string;
  userAvatar: string;
  matchName: string;
  matchAvatar: string;
  matchedAt: string;
}
