/** Return type cho useWelcomeAnimations hook */
export interface UseWelcomeAnimationsReturn {
  /** Animated style cho Floating effect (bong bóng chat) */
  floatingStyle: any;
  
  /** Animated style cho Pulsing effect (tim hoặc glow nền) */
  pulsingStyle: any;
}
